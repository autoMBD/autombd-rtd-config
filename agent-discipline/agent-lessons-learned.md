# RTD CfgFile CLI Lessons Learned

| Field | Value |
| --- | --- |
| Version | 0.3.7 |
| Date | 2026-06-30 |
| Author | autoMBD <tkung.lqk@foxmail.com> (AI-assisted) |
| Description | Running log of lessons for the RTD CfgFile CLI. Opens with the **Legacy skills baseline** (pre-project `.mex` exploration, condensed) followed by **Entries** — one per accepted agent iteration, appended by the **Reviewer** after the Tester's gate is green, so recurring failure modes become permanent guards. |

## How this log is used

- Owner: the **Reviewer** subagent. After an iteration passes the test gate, the
  Reviewer records what the iteration taught — especially anything that *passed
  the green gate but was still wrong or risky* (the gate cannot catch everything).
- Every lesson must end in a **durable guard**: a new test, a provider/asset rule,
  a domain-truth/`.xdm` sourcing requirement, a Reviewer checklist item, or a
  Worker convention. A lesson without a guard is not done.
- Keep entries short and factual: what happened → root cause → the guard that
  prevents recurrence. Newest first.

## Legacy skills baseline (pre-project `.mex` exploration)

Condensed from pre-project exploration of the deprecated S32K3 RTD 7.0.1 config
skills (`autombd-skills/skills/rtd-config/*`, plus the shared-validation and
minimal-`.mex` reference skills) — development reference only, never a runtime
dependency. These `.mex` editing lessons predate the agent loop and seed the
rules now enforced in the providers, the backend document core, the static
checks, and the domain-truth validation flow. Version-sensitive to S32K3
RTD 7.0.1; recheck before generalizing to other RTD versions or device families.

- **LSK-1 — `quick_selection` is not harmless metadata (highest-risk lesson).**
  If an element carrying `quick_selection` is edited without removing that
  marker, S32 ConfigTools can treat it as a default-template selection and
  revert the subtree, discard generated-file entries, or report the failure in
  another module. Canonical trap: leaving `quick_selection` on
  `<config_set name="Mcl">` while enabling FlexIO common / adding FlexIO logic
  channels makes ConfigTools revert the Mcl tree and surface it as a *Uart*
  out-of-range error — so the first diagnosis step is checking Mcl
  `quick_selection`, not Uart fields. Guard: the backend "mark modified" strips
  `quick_selection` from the modified element and the nearest carrying ancestor,
  keeping it only on byte-identical untouched elements (enforced in `apply.py`
  plus unit/fixture tests; see also LL-013).
- **LSK-2 — Narrow, ownership-bounded, byte-faithful edits.** Inspect and edit
  only the regions a command requires; a no-edit write reproduces the file
  byte-for-byte. Each provider writes only its owned module area; cross-module
  needs are explicit dependency requests. Preserve unrelated UUIDs, dependency
  entries, component order, localized labels, and project links. Never
  hand-minimize `generated_project_files` — let ConfigTools refresh them after
  coherent edits; review the post-validation `.mex` diff for metadata churn.
- **LSK-3 — The backend document core owns XML safety, not each provider.** The
  core provides targeted module/config-set lookup, setting/container
  lookup+upsert, mark-modified (with `quick_selection` clearing), narrow writes,
  generated-UUID support, and diagnostics (missing instance, duplicate
  names/IDs, invalid/stale refs, quick-selection conflicts). Providers describe
  *what* changes; the core applies consistent XML behavior.
- **LSK-4 — Runtime uses committed assets, never dev source material.** Do not
  load deprecated skills, Excel workbooks, raw RTD descriptors, or installed-RTD
  scans at runtime; convert needed facts into committed, versioned assets. (Now
  the spec's development release boundary.)
- **LSK-5 — Validation rigor: the vendor gate, not compilation.** Run static
  checks first, then S32DS/S32 ConfigTools headless validation; compilation is
  not proof of `.mex` validity. The pass gate is ConfigTools exit `0` **and** no
  SEVERE `[TOOL] … has the following error` — exit `0` alone is insufficient (it
  returns `0` even with SEVERE config errors). The concrete, live-verified
  S32DS 3.6.7 headless flow is recorded in
  [`rtd-config-domain-truth.md`](../docs/specs/rtd-config-domain-truth.md) §3.
- **LSK-6 — Module ownership map (cross-module needs are explicit).** Mcu owns
  clocks/modes (Uart + FlexIO depend on valid clock refs); BaseNXP/OsIf
  underpins Uart timeout (a bare-metal system timer takes an Mcu clock ref *or*
  an explicit frequency, not both); Platform owns IRQ entries (exact source,
  priority, handler or `NULL_PTR`, enable, partition/core); Port is a *generic*
  pin-mux/electrical service (not Uart-bound; preserve `UnTouchedPortPin` /
  `UntouchedIMCR`); Dio owns logical channel IDs while Port owns the pad; Mcl
  owns FlexIO common + DMA logic channels consumed by Uart; Uart owns channels
  and the refs into the above.
- **LSK-7 — RTD 7.0.1 driver facts (version-sensitive).** Async method is
  INTERRUPTS or DMA only — *polling is not a valid value*; DMA must be coherent
  across Mcl and Uart (a DMA method requires matching Mcl DMA logic channels and
  Uart Tx/Rx refs — never partial); `UartChannelId` equals the channel array
  index; LPUART hardware instances are unique across active channels; a FlexIO
  `UartHwChannelRef` must point to an existing Mcl FlexIO logic channel; FlexIO
  Uart word length is constrained to 8 bits; a Uart callback must be a valid C
  identifier (`NULL_PTR` rejected). Enforced in `checks/static.py` and the
  provider asset domains; the authoritative enum/pin/range source is each
  `<Module>.xdm` via domain-truth.

## Entries

| ID | Lesson | Root cause | Durable guard |
| --- | --- | --- | --- |
| LL-029 | The Mcu forward-harden (issue #38) passed the full green gate (762 unit + 9 integration + S32DS exit 0 / 0 SEVERE + MCU-001 black-box E2E PASS / KPI 44.22s) but a cold post-gate Reviewer found: (a) 5 files had uncommitted working-tree changes (-282/+157 lines) that materially altered the code the Tester's S32DS gate validated — `_ALL_SELECTABLE_CLOCKS` 30→20, `_insert_settings_into_struct` removed with all PLL-divisor field writes, PLLunderMcuControl remove→change-to-Enabled, quick_selection-clear dropped — meaning the Reviewer was presented with an UNVERIFIED code state different from what the gate validated; (b) the docstring claimed the "full 30-clock McuClockFrequencySelect enum" but cross-check against Mcu.xdm lines 13661-13971 revealed ~48+ distinct enum literals (GMAC0_RX/TX_CLK, GMAC_TS_CLK, QSPI_SFCK_CLK, QSPI_2XSFIF, RTC_CLK, SCS_CLK, CUSTOM, FLEXCAN_PE_CLK3_7, etc.) — neither 20 nor 30 clocks is the full enum, and the asset's `enum_domains`/`deferred_clocks` sections don't document the remaining 14+ entries; (c) three stale "13 clocks" comments in apply.py docstrings survived the forward-harden expansion (carryover from pre-issue-38 code). | (a) The Tester validated the committed revision, but the working tree carried a substantial rework (PLL write-strategy redesign + clock-list reduction) applied after the gate — the reviewer's "was this gate-run on this code?" question has no definitive answer because the uncommitted delta is large enough to change S32DS behavior. (b) The Worker sourced the enum from a SUBSET of Mcu.xdm INVALID rules (CGM0-derivable clocks) and declared it "full" without scanning the complete INVALID rule block end-to-end — a classic partial-extraction issue where the first N entries looked complete. (c) Numeric counts embedded in prose docstrings are unenforced by any test; only the code list is pinned. | (a) AGENTS.md Reviewer checklist now includes: "If uncommitted changes exist that affect production code or tests, refuse the review with a BLOCKER until they are either committed with an updated test gate re-run, or discarded — the state under review must exactly match the state the Tester validated." (b) For any asset that claims to enumerate a "full" domain from an `<Module>.xdm` INVALID/RANGE block, the Explorer MUST extract every unique literal from the entire block (not a sub-range) and record them in the asset, tagging those out-of-scope explicitly (never leave undocumented entries). Reviewer cross-checks the complete `.xdm` grep output against the asset. (c) Numeric counts in docstring prose (like "13 clocks") are anti-patterns — prefer "all known fixture-safe clocks" or "as listed in `_ALL_SELECTABLE_CLOCKS`" so the prose can't drift when the list expands; add as a Worker convention. |
| LL-028 | Issue #50 added OpenCode as the DEFAULT black-box E2E backend behind a frozen-dataclass `AgentAdapter` registry (`deploy_agent`/`prepare_workdir`/`run`/`extract_result`/`compute_kpi`) and persist-on-use agent cache (`.agent-state/e2e-preferences.json`). It passed the full green gate (726 deterministic; live `--agent opencode` MCU-001 black box `configured=true`/`validate_status=passed`/`kpi_seconds=43.91`/independently re-validated exit 0 / 0 SEVERE / 120 files; git-init isolation confirmed) and a cold post-gate Reviewer found **NO blocker**. The owner-decided persist-on-use semantics are implemented EXACTLY and pinned test-for-test in `TestResolveAgent` (explicit `--agent`→writes cache+`source="flag"`; explicit-invalid→`ValueError` AND no-write `test_invalid_agent_does_not_write_cache`; no-flag valid cache→`source="cache"`; missing/corrupt/unknown-cached→`DEFAULT_AGENT` with `test_no_flag_corrupt_cache_returns_default_without_writing` asserting the cache mtime is UNCHANGED on fallback). Boundaries are clean: `tools/deploy_rtd_skill.py` is byte-untouched (empty diff), opencode reuses `.agents/skills/` via `deploy_agent="codex"` (pinned `test_opencode_pipeline_deploys_with_codex_agent` + `test_skill_lives_at_agents_skills_not_opencode_skills` asserting `.opencode` never appears), no `.opencode/skills/` is ever created, and the owner-locked KPI window `[context_injected→check_passed]` is preserved in `compute_opencode_kpi` (validate excluded; `validation_excluded_s`/`total_span_s` demoted to diagnostics). The proven codex path is genuinely UNCHANGED: the `codex exec -s <sandbox> -c approval_policy=never --skip-git-repo-check` argv, `compute_session_kpi`, `find_codex_session_file`, `_is_plan_tool`, and `_extract_blackbox_result` are all outside the diff's removed-line set; `run_codex` only gained a `model=None` it ignores; and `run_pipeline` special-cases `if agent == "codex":` inline (preserving `session_path` from the rollout JSONL) so the codex KPI flow is byte-for-byte the old one. Header is the uniform MIT banner bumped 0.3.0→0.4.0 with an opencode-aware Description; no new source file (no missed header-skill trigger); `tests/.tmp/`+`.agent-state/` gitignored and uncommitted; no secrets/debug/TODO in the diff. **Two non-blocking findings a cold reviewer must record:** (a) `_codex_compute_kpi` (the codex adapter's `compute_kpi` field) is DEAD via the pipeline — `run_pipeline` computes codex KPI in an inline branch (to also surface `session_path`) and never calls `adapter.compute_kpi` for codex, and no test references `_codex_compute_kpi`; the abstraction is therefore non-uniform (the codex adapter advertises a `compute_kpi` the pipeline bypasses). (b) The opencode multi-`text`-event concat (`_opencode_extract_result` joins all `part.text` then reverse-scans) is only tested with the BLACKBOX_RESULT marker fully inside ONE trailing text event (`_OC_NDJSON_MULTI_TEXT`); no test splits the marker line ACROSS two streamed text events, so concatenation is exercised but its load-bearing case (a marker fragmented by streaming) is unproven — and a mid-JSON split would defeat the line-oriented `_extract_blackbox_result` anyway. | A clean, well-tested backend addition still hides two reusable traps: (a) a per-agent adapter registry invites a field that the orchestrating pipeline does not actually route through for every agent — here codex needs an EXTRA output (`session_path`, the rollout-file path) that the uniform `compute_kpi(RunResult)->dict` signature cannot express, so the pipeline branches inline for codex and the codex `compute_kpi` field becomes advertised-but-unreachable dead code (a latent inconsistency: a later refactor that "deletes the inline branch because the adapter handles it" would silently drop `session_path` and never fail a test). This is the LL-023 injected-seam family inverted — there an always-injected seam hid the real default; here an adapter field is defined but never dispatched, equally invisible to a green suite. (b) A streaming agent's structured output (OpenCode NDJSON `text` events) can fragment a sentinel line the parser anchors on; testing only the whole-marker-in-one-event case proves the concat plumbing but not the property the concat exists FOR (reassembling a split marker) — the same shape as LL-019 (the create-path collapsed onto a single happy fixture), applied to a streamed result marker. | (a) Either route codex through `adapter.compute_kpi` too (e.g. let the adapter return `{"kpi": …, "session_path": …}` or carry `session_path` on the adapter), OR delete the unused `_codex_compute_kpi` + the `compute_kpi` field on the codex adapter and document that codex KPI is pipeline-inline — an adapter field that the pipeline never dispatches for that agent is dead code and must be removed or wired (extends the LL-023 Reviewer checklist: for every registry/adapter field, grep a real dispatch call-site per agent; zero call-sites for an agent ⇒ flag it). NON-BLOCKING this iteration because the inline codex branch is the proven unchanged path and the field is harmless today. (b) Add an opencode-extract test where `BLACKBOX_RESULT {…}` is SPLIT across two consecutive `text` events (e.g. `"…BLACKBOX_RESULT {\"configured\":"` then `" true, …}"`) and assert the concat+scan still recovers it — OR, if a split marker is deemed out of scope, assert in a test/comment that the marker is required to be emitted whole in a single text event (pin the contract either way). NON-BLOCKING because the live MCU-001 run extracted correctly and codex has the identical whole-marker assumption. (c) Reviewer checklist (durable): when a backend is added behind an adapter/strategy registry, verify per-agent that (i) every adapter field is actually dispatched by the orchestrator for THAT agent (no advertised-but-bypassed methods), and (ii) any output the orchestrator needs beyond the uniform return type (here `session_path`) is carried by the abstraction, not smuggled via an inline special-case that makes the abstraction a half-truth. |
| LL-027 | The ADC delivery (RTD-MEX-ADC-001..004) passed the full green gate (620 deterministic + real S32DS exit 0 / 0 SEVERE / 125 files per case + black-box E2E all PASS, KPI all < 120 s) and a cold post-gate Reviewer found NO blocker — domain values are sourced (channel enum `P0–P7`/`S8–S23`/`VREFL_54`/`VREFH_55` with no `S0–S7`, BCTU tokens `BCTU_EMIOS_{0,1,2}_{0..22}`+`EXT_TRIG`/`AUX_EXT_TRIG`, `BctuDataDestination` `BCTU_ADC_DATA_REG`/`FIFO1`/`FIFO2`, `AdcSamplingDuration` 8..255, `BctuAdcTargetMask` bit-n=ADC-n, the BCTU-HW→`ADC_CONV_TYPE_INJECTED` rule, the LIST-must-be-SW-triggered rule) all verified line-for-line against `Adc.xdm`+`Adc_s32k344_mapbga257.epd`; the asset is loaded at runtime AND pinned by a code==asset drift test (`test_adc_json_matches_apply_code_literals`, LL-012 satisfied); headers/version/ownership/diff-hygiene all clean. Two structural facts a cold review must internalize for the BCTU family: (1) **BCTU is an in-`Adc.xdm` container family, NOT a separate module** — `BctuHwUnit`/`BctuInternalTrigger`/`BctuResultFifos`/`BctuAdcNotifications` are all children of the `AdcConfigSet` struct (siblings of the `AdcHwUnit` array), so a reviewer hunting a "Bctu.xdm" or a separate `<config_set name="Bctu">` is chasing nothing; the whole BCTU subtree is Adc-owned and lives inside `<config_set name="Adc">` (correct here). (2) The S32DS sampling-duration check is **static and clock-independent**: `AdcSamplingDuration` INVALID Range `>=8 && <=255` (`Adc.xdm` L3718-3720) gates the DERIVED integer, not the requested microseconds, and the fixture's 160 MHz ADC source clock is a ConfigTools-derived node with no editable divider in the fixture or `Mcu.xdm` — so a 10 µs request (→400 @ prescale 4) is unrepresentable and the right fix was a CASE relaxation (10 µs→6 µs→240) recorded in the test-case changelog, NOT a fabricated clock edit (the superseded v0.5.2 "lower the ADC clock via the MCU divider" line was correctly abandoned in v0.5.3). | A correct, well-grounded delivery still hides two reusable traps for the NEXT BCTU/clock-derived case: (a) the BCTU naming family looks module-shaped (its own `Bctu*` prefix and `<config_set>`-like substructure) but is descriptor-nested under Adc, so an ownership/boundary review keyed on the `Bctu` prefix would wrongly expect a separate module or a Bctu provider; (b) a microsecond-denominated KPI/spec value silently assumes the derived register field is representable, but representability is a STATIC `.xdm` range over `round(t·f/prescale)` with a FIXED (non-editable) clock — so an infeasible time is a spec/case defect, not an implementation gap, and "edit the clock to make it fit" invents a divider the fixture does not expose (would fail the vendor gate or churn an unowned module). Both are the domain-truth-vs-`.xdm` family (LL-001/LL-007/LL-012) applied to a container-naming illusion and a derived-field feasibility check. | (a) BCTU OWNERSHIP RULE (codified for future reviews): the BCTU container family (`BctuHwUnit`/`BctuInternalTrigger`/`BctuResultFifos`/`BctuAdcNotifications`/`AdcHwTrigger`) is part of `Adc.xdm` and MUST be written only inside `<config_set name="Adc">` by the Adc provider — there is no Bctu module, Bctu.xdm, or `<config_set name="Bctu">`; the only cross-module edge is the Mcl DMA logic channel a FIFO-DMA (or unit-DMA) consumes, which MUST be a declared `PlannedChange(module="mcl")` and propagated into `changed_modules` (verified: `test_changed_modules_includes_mcl`, `test_plan_bctu_is_adc_owned`, `test_plan_declares_mcl_dma_dependency`). A reviewer seeing a `Bctu*` token outside the Adc config set, or a separate Bctu provider/asset, blocker-tracks it. (b) SAMPLING-FEASIBILITY RULE: any microsecond-denominated sampling time in a spec/case must be checked for representability as `AdcSamplingDuration = round(t_us·1e-6·clk_hz/prescale) ∈ [8,255]` at some allowed prescale `{1,2,4}` against the FIXTURE's actual (derived, non-editable) ADC clock BEFORE acceptance; an infeasible value is fixed by relaxing the CASE (with a changelog row showing the derived value, as v0.5.3 does) — never by inventing a clock divider the fixture/`Mcu.xdm` does not expose. The apply path already rejects an infeasible request with `adc_sampling_out_of_range` (`test_sampling_out_of_range_rejected`); the rtd-e2e-case-designer skill must run this feasibility check when authoring/revising any ADC case. (c) Windows deploy-publish transient-lock retry (the INFRA fix that unblocked the black-box harness): `tools/deploy_rtd_skill.py` now wraps the atomic `rename`/`unlink`/`rmtree` publish in `_retry_fs` scoped to `sys.platform=="win32"` + winerror {5,32,145} with a bounded budget that re-raises genuine failures (tested in `test_deploy_rtd_skill.py`) — reusable for any cross-process FS publish on Windows; do NOT broaden the retry to all `OSError` (would mask real deploy failures). (d) Reviewer checklist (note, non-blocking here): `docs/tests/rtd-config-acceptance-report.md` still lists ADC-001..004 as **NOT RUN** — the orchestrator records the PASS+KPI evidence (50.5/75.4/71.0/58.7 s, 1 edit each, exit 0 / 0 SEVERE / 125 files) after acceptance; a Reviewer must flag a stale acceptance-report row even when the test gate is green, since the report is the durable PASS record and the gate does not write it. |
| LL-026 | Issue #14 testing left 8 orphaned `rtd-bb-*` temp directories in system `%TEMP%` (the `--keep` flag preserved workdirs for subagent analysis but post-analysis cleanup was never done), and every `tempfile.mkdtemp()` access to `%TEMP%` triggered opencode permission prompts because system temp is outside the workspace. | (1) `--keep` preserved artifacts with no paired cleanup obligation. (2) Default `tempfile.mkdtemp()` targets system `%TEMP%`, which opencode requires explicit approval to write. | All test temp artifacts stay within the repository workspace: `tests/.tmp/` is the canonical temp base (passed via `--temp-base tests/.tmp`). `--keep` is used only when a subagent immediately consumes the workdir, with mandatory orchestrator cleanup afterward. Without `--keep`, the harness auto-cleans on success. `tests/.tmp/` is in `.gitignore`. Added to AGENTS.md Testing Terminology. |
| LL-025 | The issue #13 KPI back-fill changed the canonical metric and the codex-session parser, exposing two ways a metric can pass its own tests yet mis-measure the thing it names. (1) The recorded KPI proxy was `validation_excluded_s` (`total_span` − `validate`), but the WRITTEN definition is "intent analysis + planning + implementation + file editing, excluding validation" — the proxy still counts the post-edit deliberation, the standalone `check` call itself, and the trailing report, so it OVER-counts (BASENXP-001 read a MISS partly on overhead the definition excludes). (2) The check-window detector used `\bcheck\b`, which false-matched the word "check" inside codex `update_plan` payloads (`{"plan":[{"step":"…check…"}]}`) and inside hyphenated flags (`--skip-git-repo-check`); on the real baseline an early plan step worded "…check…" anchored `check_passed` ~48 s early (33 s instead of the true ~81 s), turning a MISS into a fake near-PASS. Fixed: `kpi_seconds` is now the `[context_injected → check_passed]` window emitted first-class (`validation_excluded_s` demoted to diagnostic), `_is_plan_tool` excludes `{"plan":…}` payloads from ALL classification, and the detector requires the subcommand-flag shape `(?<!-)\bcheck\b(?=\s+--)`. | A KPI proxy that does not literally match its written definition mis-measures, and a green suite cannot catch it because the tests were written to the proxy, not the definition (in-repo-green ≠ measures-what-it-claims, the LL-018/LL-021 family applied to a METRIC). Separately, codex session logs interleave a non-shell `update_plan` tool whose free-text plan prose contains command-like words ("check"/"validate"/"--configure"); any classifier that scans the raw command string for a verb misreads plan prose as a real command unless plan-tool payloads are excluded first — the parser must distinguish a tool's PROSE from a shell INVOCATION. | (a) `tools/blackbox_e2e.py::compute_session_kpi` emits `kpi_seconds` = the `[context_injected → check_passed]` window as THE canonical KPI; `validation_excluded_s` is documented DIAGNOSTIC-ONLY and must never be quoted as the KPI (harness header + docstring + acceptance-report §1 all say so). (b) `_is_plan_tool` excludes codex `{"plan":…}` payloads from edit/validate/check classification, pinned by `test_kpi_window_ignores_check_validate_configure_inside_update_plan` (verified to read 3 s instead of 61 s on the pre-fix `\bcheck\b`). (c) the standalone-check predicate requires the subcommand-flag shape `(?<!-)\bcheck\b(?=\s+--)`, so a bare "check" in a quoted path/arg or a hyphenated flag no longer matches — pinned by `test_check_regex_requires_subcommand_flag_not_bare_word` + `test_check_regex_not_fooled_by_hyphenated_flag`. (d) Reviewer checklist: when a KPI is recorded, diff the recorded proxy against the metric's written definition word-for-word; if they differ, the number is suspect even with a green suite. |
| LL-024 | RTD-MEX-BASENXP-001 was a single self-contained edit (enable the OsIf system timer) yet missed its 1-min KPI on the first measurement (baseline 81.1 s). Root analysis of the codex session showed the cost was not the edit but CEREMONY: the SKILL.md framed every module command as a mandatory two-phase "plan → configure → validate" workflow, which induced codex to emit its own `update_plan` tool calls (5 calls / 13 plan steps) before doing one trivial edit. The Worker's KPI optimization reframed SKILL.md to present a single self-contained edit as a ONE-SHOT (`basenxp set --enable-system-timer --configure` → `validate`, with a BaseNXP one-shot example) while PRESERVING the plan-first guidance for genuine multi-module orchestration. That removed the `update_plan` ceremony entirely (5→0 calls, 13→4 steps) and re-measured 53.5 s ≤ 1 min (PASS), functional PASS unchanged (independent vendor gate exit 0, 120 files, 0 SEVERE). | Prompt/skill FRAMING is a first-order KPI lever, not just correctness scaffolding: instructing the agent to always plan-then-configure makes a third-party agent spend real wall-clock time on planning ceremony proportional to the framing, even when the task is one obvious edit. The functional gate is blind to this (the multi-phase flow produces a correct `.mex` too), so a framing-induced KPI miss looks like an irreducible agent-speed limit until the framing is examined. | (a) SKILL.md now offers a "fast path (default for a single, self-contained edit)" alongside "plan-first (for cross-module orchestration)", so a one-edit task incurs no planning ceremony while multi-module work keeps the review-before-write discipline; the additive change keeps the SKILL.md contract test green and weakens no functional contract. (b) REUSABLE PLAYBOOK for the other 8 KPI back-fills: before treating a KPI miss as an agent-speed limit, inspect the session for tool-ceremony (codex `update_plan` calls / step count) induced by multi-phase skill framing; if the case is a single self-contained edit, prefer the one-shot fast-path framing first. (c) Any such optimization must preserve the plan-first path for multi-module cases and re-confirm functional PASS via the independent vendor re-gate (never trust the agent self-report). |
| LL-023 | The issue #13 KPI back-fill exposed that `python tools/blackbox_e2e.py` had been broken since the issue #12 import reshuffle dropped `import importlib.util`, yet the whole deterministic suite stayed GREEN: every pipeline test injects the `deploy_fn` seam (and the `main()` test patches `_default_deploy`), so the REAL default-deploy path — `_default_deploy` → `_load_deploy_module`, the only `importlib.util` user — was never executed by any test. The module imports fine (import-time + all injected tests pass), but the first real deploy dies at runtime with `NameError: name 'importlib' is not defined`. The CLI's own mandated entrypoint was thus unguarded by CI while still reporting green. Fixed by restoring the import and adding a test that drives the real loader. | A universally-injected test seam (`deploy_fn`) silently removes the production default it stands in for from all coverage: once every test supplies the fake, the real `_default_deploy`/`_load_deploy_module` path has zero call-sites in the suite, so an import/regression living only on that path cannot fail any test. This is the in-repo-green ≠ runtime-truth family (LL-018/LL-020) narrowed to a dependency-injection seam: the seam that makes a unit test hermetic also makes the real wiring invisible to it. | (a) `tests/unit/test_blackbox_e2e.py::TestLoadDeployModule::test_load_deploy_module_imports_real_deploy` drives the REAL `_load_deploy_module()` (asserts it loads `tools/deploy_rtd_skill.py` through `importlib.util` and exposes a callable `deploy`), so the import can never silently regress again — verified to fail with `NameError` when the import is stripped. (b) PRINCIPLE: for any always-injected seam (`deploy_fn`/`runner_fn`/…), add at least one test that exercises the REAL default the seam replaces (the unmocked wiring), so the production default path is not left wholly uncovered. (c) Reviewer checklist: when a function takes an injectable `*_fn` default, grep the suite for a call-site that omits it — if every test passes the fake, the default is untested; blocker-track or require a real-default test. |
| LL-022 | Issue #7 reorganized the docs into Category A (`docs/`, agent-agnostic, self-contained) and Category B (`agent-discipline/` + `AGENTS.md` + `agent-discipline/subagents/`). It passed the green gate (467 deterministic; no code/assets touched) and most purity checks, but a post-gate cold Reviewer caught two content-governance defects the functional gate structurally cannot see: (1) a PRE-EXISTING agent-discipline phrase ("…tests, diagnostics, and subagent validation") survived in `docs/references/rtd-config-legacy-skills-experience.md` because the Worker's Category-A purity sweep was scoped to the files it had touched, not all of `docs/`; (2) content folded out of the deleted implementation-plan into `docs/specs/rtd-config-core-design.md` re-introduced ordering/staging wording ("the **first** seven modules … **after** the minimal system, new modules are added when…") into a spec owner-locked to milestone-free architecture altitude. Both are prose-governance, invisible to `pytest`. Fixed this pass (legacy-skills → "and validation"; the Minimal-system section rephrased to set-membership with sequencing deferred to the roadmap). Minor third: the new governance doc carried an MIT source-file banner though project `.md` docs are headerless (`# Title` + metadata table) — banner removed to match peers, and the governance rule corrected to scope the MIT banner to source files. | Category-purity and spec-altitude are CONTENT rules a passing functional gate cannot see (the LL-003/LL-009/LL-018 "in-repo green ≠ release-truth" family, lifted to documentation). A reorg purity sweep scoped to the DIFF misses residuals already living in untouched files; and folding roadmap/plan prose into a spec carries staging/ordering vocabulary across the altitude boundary unless every folded sentence is re-checked against the milestone-free rule — deferring sequencing to the roadmap in the same sentence is not enough if the sentence itself uses ordering words ("first/after/when"). | (a) The Category-A purity sweep must be REPO-WIDE over all of `docs/` (excluding frozen changelog rows and `agent-discipline/review-archive/`), not diff-scoped: grep the agent-discipline lexicon (`orchestrat\|explorer\|worker\|tester\|reviewer\|subagent\|main agent\|KPI[- ]optim\|convergence gate\|iteration loop`); the only allowed survivors are the `Subagent Prompt` column name (parsed by `tools/blackbox_e2e.py` + its unit tests + help text) and `tools/blackbox_e2e.py` itself — now codified in `agent-discipline/documentation-governance.md` (Category A purity sweep + File header convention rules). (b) When folding plan/roadmap content into `docs/specs/*`, lint for ordering/staging tokens (`first\|later\|after\|next\|then\|stage\|staged\|milestone\|M\d\|phase\|sprint`) and express scope as set-membership, not sequence. (c) New project `.md` docs follow the peer convention (headerless `# Title` + metadata table); the MIT banner is for source files only. (d) The Reviewer runs a whole-`docs/` purity + altitude pass on any documentation reorganization, not only the changed files. |
| LL-021 | An EXTERNAL cold-agent review (Codex) over the WHOLE repo caught two classes of defect that the GREEN in-repo gate (467 deterministic + real S32DS exit 0 / no SEVERE + black-box BASENXP-001) structurally could not. (1) The LL-014(b) "consider broadening the detector" guard had stayed DEFERRED, so the automated `validate` severe filter (`[TOOL] ... has the following error` only) was strictly WEAKER than the manual Tester Problems-view scan: a `CORE_CLK=160`/`HSE_CLK>120 MHz` clock-constraint violation is emitted ONLY as `From Problems view: Tool problem issue: ... target: Clocks, resource: HSE_CLK` and does NOT block codegen, so it exit-0'd with 120 files and FALSE-PASSED the automated gate. This became CRITICAL the moment the black-box harness (LL-020) shipped: BOTH the agent's own `validate` AND the Tester's independent re-gate run the SAME filter, so the only thing that had been catching this — a human running the comprehensive Problems-view scan — was now out of the loop, and a clock-out-of-range `.mex` would ship green end-to-end. (2) Accumulated staleness invisible from inside the team: `Milestone 1`/`M1` wording across runtime code including TWO factually-WRONG docstrings (`checks/static.py` advertised "Milestone 1 DMA rejection" though `_check_dma` actually validates DMA COHERENCE and DMA is supported; `backends/s32_mex/apply.py` module docstring claimed "M1 only edits EXISTING instances, never creates" though providers now create declared children — DioPort/FlexIO/DMA channels), a drift-prone hardcoded `tests-411 passing` README badge, in-body core-design doc paths missing the `docs/` prefix, and a domain-truth asset rule looser than the enforced LL-012 discipline. This pass broadened `find_severe_tool_problems` to ALSO flag the `From Problems view` + `Tool problem issue` substring pair (re-baselined on the REAL gate: pristine fixture + a valid 160/80/40 config still PASS — no false positive; the HSE_CLK>120 case is now caught with `passed=False` despite exit 0 + 120 files; the benign `Dependency … M7_0`/`Cannot get container`/SerDes/`Null toolchain`/`target: Toolchain/IDE project` lines provably still excluded because they lack the pair), swept the M1 wording + fixed the two wrong docstrings, de-numbered the README badge, corrected the core-design paths, and tightened domain-truth step 3 to the LL-012 rule verbatim. | "Consider/deferred" guards (LL-014(b)) silently ROT: a guard recorded as optional and never promoted to enforcement decays into a latent gap, and the gap stayed dormant only because a manual human step compensated for it — until LL-020 automated the executor and removed the human, turning a dormant gap into a release-critical one (the compensating control was eliminated without re-checking what it had been silently covering). And an in-repo team is BLIND to its own staleness: the authors who wrote `Milestone 1`/`411`/the wrong docstrings read past them every iteration; nothing in the convergence gate asserts prose accuracy, doc-path validity, badge currency, or spec-vs-lesson consistency, so accumulated drift is invisible from inside exactly as in-repo green ≠ release-truth (LL-003/LL-009/LL-013/LL-018/LL-020) — here lifted to the whole-repo level. | (a) The broadened detector makes the AUTOMATED gate == the MANUAL bar: `find_severe_tool_problems` now flags both `[TOOL]`+`has the following error` AND `From Problems view`+`Tool problem issue`, pinned by real-line tests (`test_find_severe_tool_problems_flags_problems_view_hse_clk` both !MESSAGE/SEVERE + EN/中文, `..._flags_peripherals_and_pins_targets`, `test_validation_outcome_pass_gate_with_problems_view_problem` asserts `passed=False` at exit 0 + 120 files, plus the benign-line regression asserting the known-good lines stay unflagged) so the LL-014 bypass cannot regress — promote any future "consider/deferred" gate guard to an enforced test the same way. (b) README test badge de-numbered (`tests-passing`, no count) so a hardcoded total cannot drift; do NOT hardcode a moving count in any active doc (the count lives only in the acceptance-report summary + its changelog). (c) domain-truth step 3 tightened to the LL-012 discipline verbatim (provider loads the asset at runtime OR embeds + pins it with a code==asset drift test; documentation-only-no-loader-no-pin prohibited). (d) When a diagnostic blocker is removed/repurposed, its docstring must be rewritten to the NEW behavior in the same pass (`static.py` DMA-rejection→coherence; `apply.py` M1-edits-only→provider-model) — re-affirms LL-010/LL-019(e) prose-drift. (e) STANDING PRACTICE: adopt PERIODIC EXTERNAL/COLD REVIEW — a fresh third-party agent reading the WHOLE repo with no project memory — as a recurring gate, applying the same black-box principle as the LL-020 E2E harness to CODE/DOC review; a green test gate certifies behavior, not prose/path/badge/spec-consistency accuracy. (f) Reviewer checklist: when a compensating MANUAL control is automated away (here the human Problems-view scan, removed by the LL-020 harness), explicitly enumerate what that manual step was covering and prove the automation covers it too — never assume an automated path inherits a human path's coverage. |
| LL-020 | The in-repo "isolated E2E" was executed by the platform's EMBEDDED subagent — fresh-CONTEXT but NOT a clean ENVIRONMENT: it inherits this repo's `CLAUDE.md`/`AGENTS.md` + filesystem, so it can peek at the specs, sources, domain-truth, and the fixtures' authoring context. That false black-box confidence let real release gaps ship GREEN through the full in-repo gate (deterministic suite + real S32DS exit 0 / no SEVERE + codegen) and be caught only by manual third-party-agent runs: LL-018 (stale Milestone-1 `SKILL.md` that nearly drove a false DMA refusal + an env-dependent `validate` that skipped the vendor gate cold) and LL-019 (the DIO unpopulated-container path with zero coverage). This pass adds a TRUE black box — `tools/blackbox_e2e.py`, an extensible third-party-agent-CLI runner (Codex proven: prompt on STDIN \| `codex exec -s workspace-write -c approval_policy=never --skip-git-repo-check --cd <workdir>`; registry `AGENT_RUNNERS` for future backends) that deploys the released skill into a fresh temp dir, copies only the case fixture, drives the agent with the verbatim Subagent Prompt + a `BLACKBOX_RESULT` suffix (timeout 3× the max catalog KPI so S32DS validation fits), and keeps the workdir on failure/timeout for root-cause. A live run of RTD-MEX-BASENXP-001 was triple-confirmed (agent self-report passed; trace shows `validate` ran + S32DS launched sandboxed → 120 files / no SEVERE; and an INDEPENDENT vendor gate on the agent-produced `.mex` returned passed / exit 0 / 120 / severe []). | Conflating "fresh context" with "true black box". The embedded subagent resets conversation state but shares the same working tree and project instruction files, so "the released skill works cold" was never actually tested — the executor could (and structurally did) draw on repo knowledge the real end user will not have. The gate measured the code in-tree and a vendor `.mex` produced by a context-isolated-but-environment-coupled agent, never the artifact as a genuinely external agent would consume it. This is the LL-018/LL-003/LL-009/LL-013 family (in-repo green ≠ release-truth) lifted to the EXECUTOR itself: the validator's environment, not just its context, must be external. | (a) `tools/blackbox_e2e.py` (Codex-proven, registry-extensible) is now the MANDATED E2E mechanism; the embedded subagent is explicitly declared NOT a valid black box, codified across `.claude/agents/tester.md`, `AGENTS.md` (orchestrator E2E line + Tester bullet + Testing Terminology), `docs/tests/rtd-config-test-strategy.md` (§2.4 + §4), `docs/tests/rtd-config-test-cases.md` (§1), `docs/specs/rtd-config-core-design.md` (workflow + success), and `docs/plans/rtd-cfgfile-cli-implementation-plan.md` (§5). (b) The Tester MUST independently re-run the vendor gate (`validate`) on the agent-produced `.mex` from the trusted environment and NEVER trust the agent's self-reported `BLACKBOX_RESULT` — the triple-confirm pattern (agent report + agent trace + independent vendor re-gate) is the acceptance evidence. (c) The harness is unit-pinned (`tests/unit/test_blackbox_e2e.py`: registry/parse/prompt/runner/pipeline wiring, no real codex/S32DS) so the recipe + parse + timeout-multiplier cannot silently regress; deterministic suite 423→464. (d) Any new E2E backend drives the deploy index + skill path from the backend, not a hardcoded codex `.agents/skills` — FIXED this pass: `run_pipeline` deploys with `(agent,)` and derives the skill dir from the `DeployResult.destination`, so a runner-registry entry + a `deploy_rtd_skill` agent mapping suffices for a new backend (codex remains byte-identical). (e) PRINCIPLE: an "isolation" claim must specify isolation of ENVIRONMENT (filesystem + instruction files), not merely conversation context; a validator that shares the repo working tree is not a black box. |
| LL-019 | The DIO add-channel capability (RTD-MEX-DIO-001) shipped GREEN through the full in-repo gate (deterministic suite + real S32DS exit 0 / no SEVERE + codegen verified) yet silently lacked support for adding a channel on a pin whose DioPort container did not yet exist: `apply_dio_set` returned a `dio_port_not_found` blocker for any pin with mscr >= 16. The gap was invisible to the gate because every DIO test AND the DIO-001 E2E case used PTA5 (mscr=5 -> DioPortId=0), which resolves to the pre-existing `DioPort_0` in the fixture, so the insertion path was only ever exercised against an ALREADY-POPULATED container. It surfaced only when a black-box isolated E2E (round 2) picked PTA30 (mscr=30 -> DioPortId=1), whose container is absent, and the consuming cold agent had to patch the provider to proceed. This pass adds DioPort container auto-creation (count existing structs -> zero-based array index; byte-faithful `<struct name="<index>">` with `Name=DioPort_<index>`, computed `DioPortId=mscr//16`, empty DioChannel/DioChannelGroup/DioPortEcucPartitionRef arrays; splice -> reload -> re-find -> fall through to channel insertion), every value byte-verified against fixture `DioPort_0` and the vendor `example_Dio.mex` precedent (struct-name/Name use the ARRAY INDEX while DioPortId is computed — they differ for PTB0: array index 1, DioPortId 2). Review cleanups applied the SAME pass: the `apply_dio_set` docstring now lists the two real codes (`dio_port_array_not_found`, `dio_port_create_failed`) instead of the dead `dio_port_not_found`; the changed test was renamed `test_dio_port_autocreated_for_port1_pin_pta16`; the port-struct field-set/order/naming were recorded in `dio.json` (`port_fields`/`port_children_order`/`port_naming_rule`) and pinned by `test_dio_json_port_fields_match_apply_code_literals` (LL-012 discipline). Deferred (unexploitable: max committed mscr 219 -> max DioPortId 13): an explicit DioPortId range (0-13) guard. | Container CREATION was never tested — only INSERTION into a container that already existed. The single happy-path pin (PTA5) collapsed two distinct code paths (find-existing vs create-missing) into one, so the create path had zero coverage, and the in-repo gate (tests + vendor codegen) only ever fed it that one pin. This is the LL-011 next-id family widened: "computed, not hardcoded" was proven for the channel id but the very EXISTENCE of the parent container was assumed; a fixture that ships exactly one of a thing hides every "create the Nth" path. The black-box E2E caught it only by luck of pin choice, not by design. | (a) The new-port + anti-hardcode tests are the durable code guard: `test_new_port_created_for_pta30`, `test_anti_hardcode_port_id_vs_array_index` (PTB0 array-index 1 vs DioPortId 2 — proves Name/struct-name use the array index while DioPortId is computed), `test_new_port_idempotent_pta30`, `test_regression_pta5_no_new_port_created`, `test_new_port_quick_selection_removed`, `test_new_port_written_file_well_formed`. (b) RTD-MEX-DIO-002 added (a pin whose DioPort container does NOT exist, PTA30 -> DioPort_1) so the create path is permanently exercised at the isolated-acceptance altitude, not left to a lucky pin choice; vendor + codegen verified (DioConf_DioPort_DioPort_1=0x01, DioConf_DioChannel_LED_CTRL=0x001e). (c) PRINCIPLE (generalize beyond DIO): container/parent CREATION must be tested against an UNPOPULATED (or absent) container, never only insertion into an existing one — whenever a fixture ships exactly one instance of a container, add a perturbed/empty-container case so the "create the next one" path cannot be satisfied by the pre-existing instance (extends the LL-011 next-id family from ids to container existence). (d) Asset discipline (LL-012): the port-struct literals are recorded in `dio.json` and pinned by a code==asset test, mirroring the channel pin. (e) When a diagnostic code is removed, grep the WHOLE module (code + docstrings + test names + SKILL.md) for it in the same pass — a removed blocker lingering in a docstring or a test name re-asserts behavior that no longer exists (LL-010 prose-drift); done here. |
| LL-018 | An in-repo 9/9 acceptance (411 deterministic + real S32DS vendor exit 0 / 120 files / no SEVERE [TOOL]) was declared COMPLETE, but a black-box isolated E2E — a cold agent given ONLY the released `autombd-rtd` skill + a fixture, never this repo — surfaced two release-readiness gaps the green in-repo gate could not see. (1) `SKILL.md` was stale Milestone-1 fiction: it claimed "interrupt is the only mode", told the agent DMA is "not in Milestone 1" and to reject it, documented the removed `dma_not_supported_in_m1` diagnostic, and documented ONLY `uart` though the tool ships seven modules (Mcu/BaseNXP/Platform/Port/Dio/Mcl/Uart) — which nearly caused a FALSE REFUSAL of a valid DMA request by the consuming agent. (2) `validate` returned `s32ds_root_not_configured` and never ran the vendor gate on a clean machine, because resolution depended on a manually-set `--s32ds-root`/`RTD_CONFIG_S32DS_ROOT` the isolated environment did not have. The in-repo suite passed throughout because the deterministic tests never asserted SKILL.md content and the vendor-gate runs were on a developer machine where the env var was always set. This pass fixed both (SKILL.md full rewrite to the 7-module surface + DMA; `find_s32ds_root` auto-discovery: explicit → env → `which(s32dsc.exe)` → `C:\NXP\S32DS*` glob, newest valid wins, ARM install excluded by the K3-PlatformSDK validity filter). Residual found-and-fixed-same-pass: two now-stale SIBLING docs the diff did not touch — `README.md` Quick start showed `validate --s32ds-root …` as the canonical call, and `README.md` Status framed DMA as a not-yet-delivered "tracked target" — so a doc-set internally consistent at the SKILL.md altitude can still be inconsistent across README/spec/plan after a capability lands. | The released skill artifact (`SKILL.md`) and the vendor gate's environment dependency were never themselves part of the convergence gate: tests asserted the CLI's behavior but not the prose the consuming agent actually reads, and the vendor gate was only ever exercised with the S32DS root pre-supplied, so "the tool works here" was conflated with "the released skill works cold". This is the LL-007 family (self-consistency ≠ release-truth) lifted from spec text to the shipped skill + its runtime environment, compounded by README/Status drift (LL-008 family) when DMA moved from "target" to "delivered". | (a) Keep the SKILL.md contract test (`test_rtd_config_skill_documents_public_cli_and_module_surface`) as the durable guard and EXTEND it toward a per-command check: it already pins the 7-module `<module> set` surface + `validate` + DMA-supported and bans `Milestone 1`/`dma_not_supported_in_m1`; consider asserting each documented diagnostic code and `--mode` value also exists in the code surface so SKILL.md cannot re-drift from `cli.py`/`static.py`. (b) S32DS auto-discovery is the durable guard for the env-dependent gate; `validate` is now env-independent on any standard install and `find_s32ds_root` is pinned with branch tests (15) — plus residual hardening landed: deterministic version+name sort tiebreak, `_DEFAULT_S32DS_PARENTS` default-path coverage, and an unparseable/ARM-named valid-root sort test so a real ARM install provably never wins. (c) Make the BLACK-BOX isolated E2E a mandatory re-run after ANY change to `SKILL.md`, the launcher, the deployed payload, or the env-resolution path — an in-repo green gate is necessary but NOT sufficient for release readiness (extends LL-003/LL-009/LL-013/LL-014: the gate must exercise the artifact as shipped, cold, not just the code in-tree). (d) Reviewer checklist: when a capability changes state (e.g. DMA target→delivered) or a flag becomes optional (manual root→auto-discovered), grep the WHOLE doc set (README Status + Quick start, specs, plan) for the old framing in the same pass — a corrected `SKILL.md` does not certify the sibling docs. |
| LL-017 | RTD-MEX-UART-003 (DMA mode, the FINAL case 9/9) was built with NO end-to-end vendor DMA example for the LPUART+DMA wiring: the dmaLogicChannel_Type_1 RX struct, the two DMATCD Platform ISRs, and the UartDmaTx/RxChannelRef population were assembled by CROSS-EXAMPLE inference (fixture dmaLogicChannel_Type_0 for the struct field-set; Platform.epd/Dma_Ip_Irq.c/Spi_Transfer for the DMATCD<N>_IRQn->Dma0_Ch<N>_IRQHandler map) and validated only by the S32DS gate OUTPUT codegen. The gate is GREEN and the assembly is in fact correct (vendor exit 0; codegen verified Uart LPUART_UART_IP_USING_DMA + DMA_LOGIC_CH_0/1, Mcl HW_CH_0/1 EnRequest=TRUE, Platform DMATCD0/1_IRQn->Dma0_Ch0/1_IRQHandler), but three non-test gaps remain. (a) LL-012 recurred a 5th time, narrowed to ONE key: of the three new uart.json keys, dma_hw_channel_irq_map (apply.py:755, platform.py:131) and dma_channel_ref_path_pattern (apply.py:814) ARE loaded at runtime -- the LL-016 discipline held for those -- but mcl_dma_channel_template is documentation-only (zero loader call-sites; grep finds it only in docstrings apply.py:454/633/846 and in tests), while the three activation flags it documents are HARDCODED value=true inline in _build_dma_logic_channel_struct_bytes (apply.py ~482/495/518) and in _activate_dma_logic_channel_0 (apply.py ~609/617). The two TestDmaAntiHardcode tests (test_uart_dma.py:762,784) are TAUTOLOGICAL -- they json.load the asset and assert it contains DMATCD0_IRQn etc., never calling apply_uart_set, so they prove nothing about the production path being asset-driven (the exact LL-016 asset-contains-not-code==asset anti-pattern). (b) checks/static.py _check_dma (still called at static.py:337) was emptied to a no-op whose docstring claims structural consistency is validated elsewhere -- but NO DMA structural check exists anywhere, so a hand-edited or partially-applied file with UartInteruptDmaMethod=USING_DMA but empty UartDmaTx/RxChannelRef or MclEnableDma=false passes static checks, leaving the Uart.xdm INVALID rule (DMA method => Tx/Rx refs required) unguarded by any deterministic check. (c) the dmaLogicChannel_Type_1 field-set/order is correct (byte-faithful mirror of fixture _0, verified line-by-line) but is only spot-checked (LogicName/HwChId/HwInstId + 3 flags); no test asserts _1 full nested struct/setting name set equals _0, so a future dropped nested field would pass deterministically and rely on the vendor gate to catch it. What the iteration got RIGHT (verified): phase ordering is textbook LL-013 -- all raw-byte splices (MCL ch1, 2 Platform ISRs, Tx/Rx ref arrays, MCU clock-ref) in Phase 1, all attribute mutations (method, UartDmaEnable, MclEnableDma, ch0 flags) in Phase 2, mark_modified+quick_selection clear LAST in Phase 3, every Phase-2 target re-found fresh after the reloads; none of the mutated config_sets/structs carry quick_selection (verified Mcl/Uart/Platform/Mcu config_sets and dmaLogicChannel_Type_0), so no mutation is lost to a reload; idempotency is byte-proven (test_dma_idempotent_file_bytes) and no-duplicate proven for ch1/DMATCD0/DMATCD1; existing LPUART3_IRQn/FLEXIO_IRQn ISRs preserved; the byte-delta bound <15000 vs measured ~9484 is justified per LL-015; the DMATCD map provenance follows the accepted instance_irq_clock_map asset-note pattern. | Two root causes. (1) A capability whose only end-to-end oracle is vendor codegen invites PARTIAL asset discipline: values that flowed through a loaded asset (irq map, ref pattern) got real loaders, but the bulk static struct template -- a verbatim copy of fixture _0 -- was authored as inline literals with a parallel documentation asset key and no equality test, identical root cause to LL-012/LL-016 (grounded-in-uart.json is unenforced prose whenever the code never reads the key); the anti-hardcode tests were written against the asset instead of against apply output. (2) When the M1 DMA-rejection blocker was removed, the check body was EMPTIED rather than REPLACED with the now-relevant structural invariant, and validated-elsewhere was assumed not verified, leaving a dead no-op advertising coverage that does not exist. | (a) Either load mcl_dma_channel_template in the struct/flag builders OR add a code==asset test that fails if the emitted dmaLogicChannel flags diverge from the template; a documentation-only asset key with zero loader call-sites and no equality test is PROHIBITED (re-affirm LL-012 a 5th time / LL-016 a 2nd time). Make the Reviewer per-key loader-grep a hard checklist gate: every committed asset key must have at least one runtime loader call-site OR a code==asset pin test, else blocker-track it. (b) Rewrite the two TestDmaAntiHardcode tests to call apply_uart_set and assert the WRITTEN ISR names/handlers equal the asset map values (perturb the loaded map and assert the output tracks it), so a future hardcode of DMATCD0_IRQn in production fails; a test that never invokes production code does not satisfy an anti-hardcode requirement. (c) Make _check_dma enforce the Uart.xdm INVALID rule: if method==LPUART_UART_IP_USING_DMA (or UartDmaEnable==true) then UartDmaTxChannelRef[0] and UartDmaRxChannelRef[0] must be non-empty AND MclEnableDma==true, else emit a blocker; add a test that a DMA file with empty refs is blocked. A check emptied to a no-op must be deleted (with its call-site) or repurposed -- never left advertising validated-elsewhere when nothing validates it. (d) Add a structural-equivalence test: parse dmaLogicChannel_Type_0 and the generated _1 and assert identical nested struct + setting NAME sets (values may differ only at Name/LogicName/HwChId/the three flags/the self-ref). (e) Fix the dma_hw_channel_irq_map _note: it cites fixture Uart_Example_S32K344 as a source for the DMATCD/Dma0_Ch handler names, but those names appear nowhere in the committed fixture (verified) -- relabel installed-RTD-derived (Platform.epd/Dma_Ip_Irq.c) per LL-016(b), and record the DMATCD->handler map in docs/specs/rtd-config-domain-truth.md (which records no ISR/IRQ facts at all) so a future edit can re-verify it without the RTD install. (f) changed_modules reports mcl only when ch1 is freshly inserted (apply.py:759), so a 2nd idempotent DMA run omits mcl though MCL attributes are re-written -- align changed_modules with the actual modified set or document it reflects first-apply deltas. |
| LL-016 | RTD-MEX-UART-002 added a FlexIO Tx+Rx channel-pair creator and, with it, FIVE new uart.json keys (enum_domains.FlexioDesireBaudrate, .FlexioBitCount, .FlexioDriverDirection, top-level flexio_channel_template field_order+defaults, and mcl_ref_path_pattern) -- but apply_uart_add_flexio_channel loads NONE of them: _load_uart_asset is consumed only for instance_irq_clock_map (the UART-001 LPUART path), while the new path CONSTRUCTS every value inline (baud_enum=FLEXIO_UART_BAUDRATE_<baud>, bit_count_enum=FLEXIO_UART_IP_<wl>_BITS_PER_CHAR, the mcl-ref path /Mcl/Mcl/MclConfig/FlexioCommon_0/<name>, and the FlexioUartInteruptDmaMethod / CustomTimerDecrement literals in _build_flexio_uart_channel_bytes). The TestUartAssetFlexioEnums block is commented (LL-012 pin) but only asserts the asset *contains* the strings (e.g. assert FLEXIO_UART_BAUDRATE_921600 in bauds); it never compares the asset to what the code emits, so the five keys are decorative documentation that can silently drift from the hardcoded values with nothing failing -- a direct third recurrence of LL-012 (after LL-013(e) Dio, LL-014(d) Mcu). The gate stayed green because vendor codegen validates the OUTPUT .mex, whose inline strings happen to be correct. Provenance is also imprecise: flexio_channel_template._note cites fixture lines 914-924 (Flexio0_Tx) but 914-924 is the Flexio0_Tx FlexioModuleConfiguration SUB-struct (the channel struct is 893-925), and it cites Uart.xdm, which is not committed in the repo and so is unverifiable. (What the iteration got RIGHT was verified: the two channels byte layout mirrors the fixture Flexio0_Tx/Flexio1_Rx exactly; the Uart UartHwChannelRef and its MCL logic-channel are BOTH derived from the SAME tx_name/rx_name variable, so they cannot diverge by construction (no two independent hardcodes); MCL ids are computed max+1 and TestAntiHardcode.test_new_mcl_ids_dynamic perturbs the fixture (PRE_EXISTING) to prove CHANNEL_3/CHANNEL_4 not 2/3; idempotency is byte-proven across reapply; plan() declares uart+mcl+platform+mcu owners with concrete FLEXIO_IRQn/MCL_FLEXIO_ISR/FLEXIO_CLK descriptions (LL-010); the byte-footprint bound <11000 is justified against the measured ~7343 B (~1.5x, ~5.7% of the 192 KB file) per LL-015; the UART-001 LPUART path is a pure-addition diff, untouched.) | The asset was authored as a parallel documentation artifact while the channel/enum values were hand-written as Python literals, with no loader for the new keys and no test asserting code==asset -- identical root cause to LL-012/LL-013(e)/LL-014(d): grounded-in-uart.json is unenforced prose. The (LL-012 pin) label was attached to asset-presence assertions, which cannot catch code/asset drift because the property an LL-012 pin must guard (the value the code EMITS equals the value in the asset) is never expressed when the code never reads the asset. | (a) Make the five new keys the runtime source of truth or pin them: either apply_uart_add_flexio_channel loads enum_domains.FlexioDesireBaudrate (validate the requested baud is a member; reject otherwise), .FlexioBitCount/.FlexioDriverDirection, flexio_channel_template.field_order+defaults, and mcl_ref_path_pattern (format with the channel name) -- OR add a code==asset test that fails when the inline baud_enum/bit_count_enum, the _build_flexio_uart_channel_bytes field-set+order, or the mcl-ref pattern diverge from the asset. A documentation-only asset with no loader and no equality test is prohibited (re-affirm LL-012 a 4th time; add one cross-module Reviewer checklist item: for every committed asset key, grep its loader call-sites -- zero call-sites => blocker-track it). (b) Asset provenance must be precise and verifiable: _note line refs must bracket the element they name (the Flexio0_Tx CHANNEL struct is 893-925, not 914-924), and a <Module>.xdm citation must reference a file actually committed (Uart.xdm is absent) or be relabeled fixture-derived. (c) Carry the LL-011 own-computed-id proof to UartChannelId: test_new_uart_channel_ids_dynamic runs only on the unmodified fixture (0,1,2 -> 3,4), so a regression hardcoding the next id to 3/4 (or to len(existing)) would pass -- add a perturbed-fixture case (pre-insert a higher-id channel) that forces UartChannelId beyond 4, mirroring the MCL test. (d) Cross-reference-derivation guard (verified right here, make it durable): when one created element references another created in the same call, both endpoints must derive from ONE shared name variable, and a test must pin the ref by passing CUSTOM --tx-name/--rx-name and asserting the Uart UartHwChannelRef tracks the renamed MCL channel (current tests only assert the literal default UART2_TX/UART2_RX, which a divergent hardcode of the default could still pass). (e) Drop the dead uart_edited_tx/uart_edited_rx assignments (apply.py ~3318/3326 -- the bool return is never read) or use it to make changed_modules accurate: the function hardcodes changed_modules=[uart,mcl] and unconditionally mark_modified-s Platform/Mcu even on idempotent no-ops, contradicting its own docstring (platform/mcu only if absent); harmless today only because the <config_set> tags carry no quick_selection, so the pop is a byte no-op. |
| LL-015 | RTD-MEX-UART-001 landed the first three-module orchestration (`apply_uart_set` now writes Uart channel + Platform ISR + Mcu clock-ref in one call, precedent DIO-001 writing Port), and to accommodate it the narrowness guard `test_owned_edit_touches_only_changed_lines` was relaxed from `<=8` to `<=50` — but the bound was driven onto the FULL orchestration intent (`LPUART_0` interrupt) whose real footprint is only **15 changed lines** (UartHwChannel + UartClockRef edits ~2, the inserted PlatformIsrConfig struct ~8, the inserted McuClockReferencePoint struct ~5). A `<=50` ceiling is ~3.3x the real diff, so a future regression that re-emits an entire array or duplicates a struct block (up to ~40 churned lines) would still pass the narrowness guard green. The gate stayed green because exit-0 + 15-line diff are both well under 50; the loosened bound is the latent risk, not the current diff. (Everything the iteration got RIGHT was verified: `_load_uart_asset` is a genuine runtime dependency in both `apply` and `cli.normalize_uart_intent`, `TestUartAsset` pins it (LL-012 satisfied); `TestAntiHardcode.test_lpuart_5_yields_correct_irq` drives a different instance so a hardcoded `8`/`AIPS_PLAT_CLK` fails (LL-011/LL-013 satisfied); idempotency is byte-proven across write/reload/reapply; FLEXIO correctly skips Platform/Mcu; `plan()` declares the platform+mcu owners (LL-010).) | A single narrowness assertion was forced to cover two different change magnitudes at once: it historically guarded a SIMPLE attribute-only channel edit (~2-8 lines) but was retargeted to the full cross-module orchestration (~15 lines), and the bound was set to a round number (50) with comfortable headroom rather than to the measured footprint. As cross-module orchestration grows, a single coarse upper bound stops discriminating between "narrow splice" and "partial reserialization / duplicate block". | (a) Tighten the bound to reflect the MEASURED orchestration footprint: `<=20` for the `LPUART_0` full-orchestration intent (15 lines actual + small headroom) so a ~40-line regression fails — a 50-line ceiling on a narrowness guard is a finding. (b) PREFERRED: split the guard into two assertions — keep a tight `<=8` bound on an attribute-only edit (e.g. baud-only on an already-interrupt channel, no insertions) AND add a separate `<=20` bound for the full 3-module orchestration — so each change class has a bound that reflects its real magnitude and neither can hide behind the other. (c) Reviewer checklist: whenever a narrowness/diff-size bound is RELAXED to accommodate new scope, the new bound must be justified against an empirically measured changed-line count (record it in the test comment), not a round number; the headroom over the measured value should be small (≈1.3x), never multiples. (d) Re-affirm LL-012 for Uart (asset IS loaded at runtime + pinned by `TestUartAsset`, including the per-value provenance that `LPUART_0`/`LPUART_8`→`AIPS_PLAT_CLK` vs all others→`AIPS_SLOW_CLK` and the `LPUART<n>_IRQn`/`LPUART_UART_IP_<n>_IRQHandler` map are grounded in `S32K344_IRQ.h`/`Platform.xdm`/the fixture clock tool, not invented) and LL-011/LL-013 (the instance→IRQ/handler/clock map has an anti-hardcode test on a different instance). |
| LL-014 | The exit-0 vendor gate plus the automated `[TOOL] ... has the following error` marker-scan BOTH passed while the 160 MHz Mcu clock tree still violated a Clocks-tool resource constraint: with `CORE_CLK=160` and the fixture's `MC_CGM_MUX_0_DIV3.scale=1`, `HSE_CLK=160 MHz` exceeds its 120 MHz limit, but ConfigTools surfaced this ONLY as `SEVERE: From Problems view: Tool problem issue: "...", target: Clocks, resource: HSE_CLK` (localized message: input frequency must be less than or equal to 120 MHz) and still generated code with exit 0. The first two MCU-001 vendor runs were therefore wrongly green; the violation was caught only when the Tester ran the comprehensive Problems-view SEVERE scan, which then drove the `DIV3.scale=2` fix (ffc6ff2). Separately (review-only, gate stayed green): the recipe asset `assets/nxp/s32k3/mcu/clock.json` is documentation-only — its loader `_load_mcu_clock_asset` is dead code (zero call sites), all recipe literals (`McuPllDvRdiv/Mfi/Odiv2`, `McuClkMux0DivN_Divisor`, the DIV scales, the GAP-1/2 `McuNoPll`/`McuPll0UnderMcuControl` writes) live as Python literals, and the asset silently drifted from the code across faeb1a7/ffc6ff2 (it still lists only DIV1/DIV2 and omits DIV3/DIV4/DIV6 and the GAP fixes) because the sole asset test (F1) pins only `all_selectable_clocks` — a direct LL-012 recurrence. | The automated severe detector keys on the `[TOOL] ... has the following error` string, but Clocks/Peripherals/Pins resource-constraint violations are emitted by ConfigTools in the *Problems view* form (`From Problems view: Tool problem issue: ... target: Clocks, resource: <node>`) and do NOT block codegen, so exit 0 + marker-clean was treated as a pass even though a clock node was out of range. The asset drifted because nothing loads it and only one of its keys is pinned by a test, so the recipe section is unenforced prose (root cause identical to LL-012). | (a) For ANY clock-touching case the Tester's pass gate MUST run the COMPREHENSIVE Problems-view SEVERE scan (`target: Clocks|Peripherals|Pins`, capturing the `resource:` node) in addition to exit 0 and the `[TOOL]` marker — exit 0 + marker-clean is necessary but NOT sufficient (extends LL-003/LL-009/LL-013). (b) Consider broadening the severe detector to also match `From Problems view: Tool problem issue: ... target: Clocks|Peripherals|Pins`, while continuing to EXCLUDE `target: Toolchain/IDE project`, SLF4J/NLS noise, `Cannot get container`, SerDes, and the benign `Dependency ... M7_0 not found` cross-tool notes; assess the false-positive set on a known-good baseline before changing the gate. (c) Add a deterministic test that pins `MC_CGM_MUX_0_DIV3.scale==2` for the 160 MHz recipe (done: test A3b) so the HSE_CLK guard cannot silently regress; pin every safety-relevant divider/PLL literal by test or asset. (d) Re-affirm LL-012 for Mcu: either `apply_mcu_set` loads `clock.json` at runtime, or a code==asset test pins the FULL recipe (clock_settings changes/inserts/removes, PLL params, divisors, GAP fixes) so code/asset drift fails the gate; until then, delete the dead `_load_mcu_clock_asset` loader and do not advertise the asset as the source of truth. |
| LL-013 | The exit-0 vendor gate passed while the inserted `DioChannel LED_CTRL` was ABSENT from generated code: the fixture carries `quick_selection="DioDefault"` on `<config_set name="Dio">`, which makes ConfigTools emit the factory-default Dio configuration and silently ignore every spliced channel, so `DioConf_DioChannel_LED_CTRL` never appeared in `Dio_Cfg.h` even though the `.mex` was well-formed and validation returned exit 0 with zero SEVERE. Separately (caught in review, not by the gate), the mscr→`DioPortId`(//16)/`DioChannelId`(%16) derivation — the core domain rule, verified against vendor `example_Dio.mex` (gpio,29→port1/chan13; gpio,50→port3/chan2) — has no anti-hardcode test: every passing test uses only PTA5 (→0/5), and a Reviewer mutation to `dio_port_id=0; dio_channel_id=5` passed all 21 tests; and `dio.json` repeats LL-012 (loader `_load_dio_asset` is dead code, all values are Python literals, and `test_dio_json_asset_has_correct_schema` checks asset self-consistency, not code==asset). | The `quick_selection` clear (an `attrib.pop`) was issued before a later `replace_element_region`, whose reload re-parses the tree and re-snaps `src.attrib` from raw bytes that still contain `quick_selection="DioDefault"`; `_render_minimal` then sees `element.attrib == src.attrib` and strips nothing, so the clear was lost. The gate only asserted exit 0 / well-formedness, never that the edit reached GENERATED code, so a config the tool "wrote" but the compiler never saw still scored green. The id-derivation and asset-equality were asserted only in prose/fixture-trivial values, so a hardcode or code/asset drift was invisible. | (a) Clear `quick_selection` on the carrier AFTER all insertions/reloads — it must be the last mutation before `write()`, never before a subsequent `replace_element_region` (verified BaseNXP/MCL/Port already comply). (b) The Tester's pass gate must verify the edit appears in GENERATED code (a codegen macro / pin assignment), not just exit 0, for EVERY insertion case; exit 0 + zero SEVERE is necessary but not sufficient (extends LL-003/LL-009). (c) A deterministic proxy test asserts the carrier's `quick_selection` is removed from the WRITTEN bytes (Test 7b) — mandatory for every quick_selection-bearing `config_set` an insertion touches. (d) Carry the LL-011 next-id/derived-value proof to the module's OWN computed ids, not just borrowed Port ids: add a test where a second pin yields a different mscr-derived `DioChannelId`/`DioPortId` so a hardcode fails. (e) Re-affirm LL-012: a committed asset must be loaded at runtime or pinned by a code==asset test; delete dead loaders like `_load_dio_asset`. |
| LL-012 | The Port asset `assets/nxp/s32k3/port/portpin.json` mis-attributes its provenance and is never loaded at runtime, yet the gate stayed green because every written value happens to match the fixture. (a) The asset declares `PortPinInitialMode` `"default": "PORT_GPIO_MODE"` with `source_ref "Port.xdm:PortPin/PortPinInitialMode"`, but `PORT_GPIO_MODE` is **not** in that field's `Port.xdm` RANGE (`PORT_PIN_MODE_ADC/_CAN/_DIO/...`) and the xdm DEFAULT is `PORT_PIN_MODE_DIO`. The real source of `PORT_GPIO_MODE` is the Pins-tool fixture (`Uart_Example.mex` uses it 4x; `PORT_PIN_MODE_DIO` 0x), so the value is operationally correct but the `.xdm` citation is false. (b) `apply.py` hardcodes the entire PortPin field set/order/defaults, the `<pin>` TX/RX templates, `_PACKAGE_PIN_FIELD`, and the Name convention as Python literals; `portpin.json` is referenced only in docstrings/comments ("grounded in portpin.json") and is read by no runtime code. The two truths can drift with nothing to catch it. | Domain-truth cross-check stopped at "the field name exists in `Port.xdm`" and never verified the specific *value* against the field's RANGE/DEFAULT; the ECUC `PortPinInitialMode` enum (which excludes `PORT_GPIO_MODE`) differs from the ConfigTools Pins-tool GUI mode label, and the asset conflated the two sources under one `source_ref`. The asset was authored as a documentation artifact in parallel with hand-written code literals, with no loader and no test asserting code==asset, so "grounded in portpin.json" is unenforced prose. | (a) Asset provenance must cite the *actual* source per value: a value that comes from the fixture/Pins-tool (e.g. `PortPinInitialMode=PORT_GPIO_MODE`) is labeled as fixture-derived, and any `source_ref "<Module>.xdm:..."` must name a value that is in that field's `.xdm` RANGE (or is the `.xdm` DEFAULT). Reviewer cross-checks the *value*, not just the field name, against RANGE/DEFAULT. (b) A committed asset that claims to be a template must be the runtime source of truth or be removed: either `apply_port_set` loads `portpin.json` for the field set/templates/package map, or a test pins the code literals against the asset so divergence fails the gate. A documentation-only "grounded in X" asset with no loader and no equality test is prohibited. (c) Recurrence of LL-011 in Port (dynamic `PortPinId=max+1`/struct-index are correct in code — Reviewer perturbed fixture ids 1-4→11-14 and got 15/16 — but no `test_port_apply.py` case perturbs the fixture, so a hardcoded 5/6 would pass all 18 tests): carry the LL-011 next-id-proof guard into **every** insertion module, not just MCL. |
| LL-011 | The next-available id computation for FlexIO logic channels (`apply_mcl_set`: new struct index, `FlexioMclChannelId`, `FlexioMclPinId`) is genuinely dynamic in the code — it scans existing entries and picks max+1 — but every committed test in `test_mcl_apply.py` runs only on the unmodified fixture (2 entries, so the answer is always index 2 / CHANNEL_2 / PIN_2). A regressed implementation that hardcoded index=2 / CHANNEL_2 / PIN_2 would pass all 10 tests, including idempotency (which short-circuits on the Name match before any id is computed). The vendor gate also stayed green because it validates a single insertion on that same fixture. | The "computed, not hardcoded" requirement was asserted only in prose (the module docstring and the asset `next_id_algorithm` field), never pinned by a test that changes the existing-entry set, so the property is unguarded against a future refactor. The new helpers (`_extract_channel_index` / `_extract_pin_index` malformed-to-None path, `_build_flexio_channel_struct_bytes`, `_detect_struct_indent`) and the empty-array `else` populate branch in `apply_mcl_set` likewise have no direct or branch coverage. | (a) Add a test that performs two distinct adds (or pre-seeds a higher-index entry) and asserts the second lands at CHANNEL_3 / PIN_3, proving the scan so a hardcoded id fails the gate (Reviewer verified at runtime: a second distinct add yields struct 3 / CHANNEL_3 / PIN_3). (b) Add a direct test for `_extract_channel_index` / `_extract_pin_index` on malformed input (returns None) and a test exercising the empty FlexioMclLogicChannels populate branch. (c) Reviewer checklist: when an acceptance value equals the fixtures trivial next value, require a test that perturbs the fixture so "computed" cannot be satisfied by a constant. |
| LL-010 | A module provider's `plan()` description and its declared ownership went stale after the apply path was corrected: the green gate stayed green while `basenxp.plan()` still told the user it would write `OsIfSystemTimerClockFreq=48000000` with "no core-clock ref in this project", even though `apply_basenxp_set` now writes a populated `OsIfSystemTimerClockRef` (Mcu `FLEXIO_CLK`/CORE_CLK) + an empty Freq array. The plan also never declared the cross-module Mcu read-dependency it now relies on. Separately, the fix to `apply.py`/`osif.json` was made by the **Tester**, whose charter is "edit tests only; report production gaps". | The convergence gate exercises `apply()` (and the vendor `.mex`) but nothing asserts `plan()`'s emitted `description`/`owner` or the declared dependency set, so plan and apply drifted with no failing test. The role deviation happened because a correct, fast production fix was reachable mid-gate and the boundary was not re-stated in the Tester handoff. | (a) Add a test that pins `BaseNxpProvider.plan()` output: its description must match what `apply` writes (populated ClockRef, empty Freq array, no `48000000`), and the plan must declare the Mcu `McuClockReferencePoint` dependency (owner/needs), so plan-vs-apply drift fails the gate. (b) Architecture rule: a cross-module READ that an edit depends on (here Mcu clock-ref discovery) must be an explicit declared dependency in `plan()`, not implicit in apply. (c) Process guard: when the Tester finds a production gap it reports it for the Worker; if the orchestrator authorizes an exceptional Tester-side production fix, a **fresh independent Tester** re-runs the gate (done here) AND the Reviewer treats any Tester-authored production diff as a required focus area. |
| LL-009 | The vendor gate silently failed on *every* input — even a pristine, vendor-authored fixture returned exit 2 — so "44 passed" never proved any `.mex` was vendor-valid. | Validation used the registration-based Flow A whose CDT `-import` step timed out (exit 124); the project was then unregistered, so `-UpdateCode` failed with `Cannot get container for IPath`. It went unnoticed because vendor validation is gated behind an off-by-default env flag and was never re-run on a known-good baseline. | Adopted the verified standalone **Flow B** (`-Load`/`-ExportSrc`, no registration; domain-truth §3). Pass gate now also requires code generation (`generated_files > 0`). The gate's own acceptance evidence is a known-good baseline (pristine fixture → pass) **and** a known-bad probe (invalid OsIf → SEVERE `[TOOL] … has the following error`), confirming exit 0 alone is insufficient. |
| LL-008 | Milestone/schedule wording repeatedly leaked into specs and diagrams ("M1", "first/later") across four review rounds, and changelog history was once collapsed during a doc slim-down. | Authors optimized docs locally without altitude rules: specs absorbed plan/stage detail; history was treated as compressible content. | `AGENTS.md` Documentation Boundary now mandates: specs/diagrams carry no milestone/stage/time wording (stages live only in the roadmap), and changelogs are append-only (never merged). Reviewer checks both per iteration. |
| LL-007 | Spec internal-consistency is not vendor-truth: three review rounds never caught the wrong polling/validation facts. | Reviews checked self-consistency, not the vendor source. | Reviewer cross-checks every domain value against the module `<Module>.xdm`; vendor S32DS gate is an early, mandatory step, not a late one. |
| LL-006 | `.mex` writer reserialized the whole file (~3000-line churn) yet passed well-formedness tests. | Mandatory "narrow edits" rule had no test guard. | Byte-faithful narrow-writer + regression tests (no-edit write is byte-identical; owned edit touches only changed lines). Every spec "must" maps to a test. |
| LL-005 | A runtime asset (`pins.json`) was a hand-made stub, not derived from source, and a test asserted against it. | Asset fabricated instead of built from the authoritative source. | Assets are built from source (pins from the pin-mux Excel, package-specific; per-module truth from `<Module>.xdm`); a capability is gated until its asset is real; tests never assert against a stub. |
| LL-004 | The uniform-file-header skill was never applied (54 `.py` files); a relevant skill trigger was missed, including by implementation subagents. | Skills do not auto-fire and subagents do not inherit the obligation. | Worker applies header on every new source file; Reviewer checklist includes "missed skill triggers"; cross-cutting conventions are injected into every subagent brief. |
| LL-003 | "ConfigTools exit 0 = pass" accepted configs that still had SEVERE errors. | Exit code alone is not the vendor verdict. | Pass gate = exit 0 **AND** no SEVERE `[TOOL]` resource problem (domain-truth §3); the Tester records the exit code + SEVERE count. |
| LL-002 | S32DS headless validation was non-functional as documented (hung; wrong app id / launcher / sdkPath; unregistered project). | The validation command was assumed, not verified against the installed tool + its docs. | Verified S32DS flow captured in domain-truth §3 from the official CLI docs; the Tester runs it on real hardware before claiming a vendor pass. |
| LL-001 | The tool wrote an invented Uart enum `..._USING_POLLING`; ConfigTools rejected it as "value not available". | An enum value was assumed instead of sourced from the module descriptor. | Per-module valid values come from `<Module>.xdm` and live in the provider; the Worker never invents enum strings; the Reviewer verifies against the `.xdm`. |

## Changelog

| Date | Version | Description |
| --- | --- | --- |
| 2026-06-30 | 0.3.7 | Added LL-029: Mcu forward-harden review — uncommitted delta unverified by S32DS gate, "full enum" overstated (~48+ actual), stale "13" counts in docstrings. |
| 2026-06-30 | 0.3.6 | Added LL-028: issue #50 OpenCode-default backend — clean review (persist-on-use cache + `.agents/skills/` reuse + unchanged codex path all verified); two non-blocking findings: codex `adapter.compute_kpi` is dead (pipeline branches inline for `session_path`), and the opencode split-marker NDJSON concat case is untested. |
| 2026-06-23 | 0.3.5 | Added LL-027: ADC delivery (ADC-001..004) clean review — BCTU is an in-`Adc.xdm` Adc-owned container family (no separate module); `AdcSamplingDuration` feasibility is a static `.xdm` range over a fixed derived clock (10 µs→6 µs was a case relax, not a clock edit); Windows deploy transient-lock retry. |
| 2026-06-17 | 0.3.4 | Added LL-026: test temp artifacts stay within workspace (`tests/.tmp/`, no `%TEMP%`). |
| 2026-06-17 | 0.3.3 | Added LL-025: KPI metric mis-measured — `validation_excluded_s` proxy over-counted, `\bcheck\b` false-matched plan prose. |
| 2026-06-17 | 0.3.2 | Added LL-024: multi-phase SKILL.md framing induced `update_plan` ceremony hurting KPI. |
| 2026-06-17 | 0.3.1 | Added LL-023: `import importlib.util` regression undetected — injected seam hid production path from CI. |
| 2026-06-15 | 0.3.0 | Absorbed legacy-skills `.mex` experience as `## Legacy skills baseline` LSK-1..7; deleted standalone file. |
| 2026-06-15 | 0.2.9 | Added LL-022: doc reorg content-governance defects (diff-scoped sweep, ordering wording in spec). |
| 2026-06-15 | 0.2.8 | Issue #7: moved from `docs/common/` to `agent-discipline/`. |
| 2026-06-14 | 0.2.7 | Added LL-021: deferred gate gap + in-repo staleness; broadened severe detector, adopted periodic external review. |
| 2026-06-14 | 0.2.6 | Added LL-020: embedded subagent is NOT true black box; `tools/blackbox_e2e.py` mandated. |
| 2026-06-14 | 0.2.5 | Added LL-019: DioPort container creation path untested (only exercised populated container). |
| 2026-06-13 | 0.2.4 | LL-018 review cleanups: `find_s32ds_root` tiebreak, SKILL.md contract test modernized, 411→416 tests. |
| 2026-06-13 | 0.2.3 | Added LL-018: stale SKILL.md + env-dependent `validate` surfaced by black-box cold run. |
| 2026-06-13 | 0.2.2 | Reworded LL-014 vendor error message to English. |
| 2026-06-13 | 0.2.1 | Added LL-017: DMA mode (UART-003) — cross-example assembly, doc-only asset, noop `_check_dma`. |
| 2026-06-13 | 0.2.0 | LL-016 review cleanups: FlexioDesireBaudrate loaded, anti-hardcode tests, 302→321 tests. |
| 2026-06-13 | 0.1.9 | Added LL-016: 5 `uart.json` keys never loaded (3rd LL-012 recurrence). |
| 2026-06-12 | 0.1.8 | Added LL-015: narrowness guard `<8`→`≤50` too loose for 15-line orchestration. |
| 2026-06-12 | 0.1.7 | Added LL-014: HSE_CLK>120MHz false-pass + doc-only `clock.json` drift. |
| 2026-06-12 | 0.1.6 | Added LL-013: `quick_selection` clear lost to `replace_element_region` reload. |
| 2026-06-12 | 0.1.5 | Added LL-012: `portpin.json` mis-cites `.xdm`, doc-only asset with dead loader. |
| 2026-06-11 | 0.1.4 | Added LL-011: FlexIO next-id dynamic but every test uses unmodified fixture. |
| 2026-06-11 | 0.1.3 | Added LL-010: `plan()` drift + Tester-side production fix. |
| 2026-06-11 | 0.1.2 | Added LL-009: vendor gate silently failed (Flow A timeout); adopted Flow B. |
| 2026-06-10 | 0.1.1 | Added LL-008: spec altitude + changelog integrity rules. |
| 2026-06-03 | 0.1.0 | Created log; seeded LL-001..007 from M1 Uart-reference work. |

## LL-030 - Platform/Mcl KPI Fast-Path Review (2026-07-04)

What happened -> The Platform/Mcl KPI optimization produced fresh green black-box evidence, but cold review still exposed non-test risk: Mcl's "next-available" FlexIO allocation is described as generic while the apply path uses fixture-shaped `max + 1` ids without checking the XDM enum bounds/gaps, and the Platform/Mcl assets still need explicit `_coverage` inventories before Reviewer can prove the full editable surface is implemented or deferred. Root cause -> KPI work optimized the agent route and plan wording, while tests mostly proved the current fixture and route discipline rather than descriptor-bound edge cases and asset surface accounting. Durable guard -> Any KPI fast path that claims generic module behavior must carry XDM-bound generality tests for gapped/exhausted enum domains, and each touched per-module asset must include `_coverage` before approval so deferred surface is recorded instead of hidden behind green E2E.

## LL-031 - Reference Metadata Version Drift (2026-07-07)

What happened -> Review caught module reference changelogs advancing while the metadata table still said `Version 0.1.0`, and the contract test explicitly asserted the stale value. Root cause -> The split/reference tests checked that references existed and carried payload details, but did not tie the top metadata version/date to the newest changelog row. Durable guard -> Reference contract tests should parse each reference's first changelog row and require the metadata `Version`/`Date` to match it whenever that reference is edited.

## LL-032 - Bundle Gate Must Own Every Asset Read (2026-07-13)

What happened -> The exact-bundle resolver and its focused tests were green, but cold review found that the public command path still normalized UART/BaseNXP intents from fixed-path assets before bundle preflight, while provider/apply helpers continued to reopen fixed asset paths after preflight; the resolver's own asset reader also rejected only `Path.is_symlink()` and therefore did not fail closed on Windows junction/reparse or mount-point components. Root cause -> Compatibility resolution was added as a gate beside the legacy asset-loading graph instead of replacing that graph with one resolved-bundle capability, and manifest-file hardening was mistakenly treated as equivalent to hardening every manifest-declared asset component. Durable guard -> A project command may obtain runtime assets only from its `ResolvedAssetBundle`, passed into normalization, planning, and apply; a structural test must make every legacy/fixed asset loader unreachable and prove an unsupported bundle fails before any asset-derived normalization. Every manifest-declared asset must be captured through the same no-follow/reparse/mount-aware snapshot primitive as the manifest, with injected and real symlink/junction tests for intermediate directories and the final file; lexical `..` rejection plus `Path.is_symlink()` is not a complete containment proof.

## LL-033 - Atomic Restore Is a Second Adversarial Publish (2026-07-13)

What happened -> The configure transaction passed its green gate and its Windows `ReplaceFileW` syscall-window test, but cold review found that the recovery path treated restore as trusted cleanup: it exchanged the displaced file back without compare-and-swap validation of the current destination, deleted the newly captured rescue without proving it was the rejected candidate, and the transaction's generic cleanup could delete the POSIX displaced file when restore itself failed. The transaction also had no post-publish recheck of auxiliary metadata or the target after its final directory-flush seam, so state could change after the last pre-publish check or after the adapter's snapshot and still produce PASS. Root cause -> The design correctly made forward publication linearizable but modeled rollback, cleanup, and success reporting as non-adversarial epilogues; tests exercised one swap at the forward syscall only, not a second swap at restore, restore failure through the owning transaction, or mutation at every seam after validation. Durable guard -> Treat every restore as another atomic publication: capture the actual current destination, verify both restored and rescued identities/hashes, preserve every unexpected file, and transfer cleanup ownership only after those proofs. A transaction-level fault matrix must inject swaps before forward exchange, before restore, during restore failure, after publish verification, during durability flush, and between auxiliary revalidation and publish; PASS requires a final target+auxiliary snapshot matching the committed candidate/context, while every failure asserts that no attacker-controlled or displaced file was unlinked. Platform tests must run both Windows `ReplaceFileW` and POSIX `renameat2`, including published mode/metadata preservation.

## LL-034 - Bounded Validator Output Must Preserve the Verdict (2026-07-14)

What happened -> The isolated validation lifecycle passed deterministic tests, but cold review found that bounded stdout/stderr retained only the tail while `ValidationOutcome.passed` ignored truncation and reader failure, so an early qualifying SEVERE line could be evicted by later launcher noise and turn vendor exit 0 into a false PASS. The process-tree runner also treated best-effort descendant cleanup as success on several paths: normal POSIX completion sent only SIGTERM and ignored surviving readers/processes, while interrupt cleanup could wait forever after a failed tree termination. Root cause -> Output bounds and cleanup were modeled as diagnostics around the vendor result instead of integrity conditions of that result; tests proved truncation and timeout independently but never composed a qualifying error with truncation, a reader fault, or a descendant that survives normal parent exit. Durable guard -> Vendor PASS requires complete trustworthy verdict extraction: detect qualifying sentinels while streaming or fail closed on any truncation/read failure. Every runner exit path must use one bounded terminate/escalate/reap routine, verify no owned reader or process-tree member remains, and return `process_tree_kill_failed` rather than suppressing cleanup uncertainty. Add adversarial tests for early-SEVERE-plus-tail-overflow, drain failure, a normal-exit parent with a signal-ignoring descendant, interrupt-time termination failure, owned-project lease release on every preflight failure, and workspace-component swaps during creation/materialization.

## LL-035 - Ownership Authorization Must Be Byte-Complete and Path-Narrow (2026-07-14)

What happened -> The provider registry and XML ownership audit passed a 1267-test deterministic gate, but cold review found that the audit intentionally normalized away comments, whitespace, attribute order, sibling order, and element namespace URIs. A byte-different candidate could therefore publish with zero delta entries and empty `changed_modules`; changing an element QName could be semantically significant while remaining invisible because paths retained only the local tag. The Uart binding also authorized dependencies at whole-module/whole-`config_set` granularity while the plan's concrete `PlannedChange.path` was ignored, so an always-declared Mcu clock dependency authorized unrelated Mcu settings and the tests explicitly demonstrated that any change in the broad owner/region passed. Root cause -> The audit conflated semantic comparison for user-friendly reporting with a security authorization journal, and treated module/region membership as sufficient capability despite plans already carrying narrower paths. Durable guard -> A byte-different candidate must never yield an empty authorization journal: either account for every byte-affecting token (including QName, comments, order, and significant whitespace) or fail closed when normalized XML is equal but bytes differ. Region classification must use exact ancestor context and full QName, not local tag alone. Cross-module writes must match both a binding capability and a plan-declared path/pattern; an idempotent dependency grants only its exact ensure target, never the rest of that module. Add adversarial tests for comment-only, namespace-only, reorder-only, duplicated-sibling, nested fake `pins`/`clock_settings`, and an unrelated Mcu edit under a valid Uart clock plan. Registry construction must also validate normalizer/apply signatures and runtime field types, and every newly created Python file must carry the complete uniform bilingual MIT header.

## LL-036 - Descriptor Regeneration Does Not Prove Coverage Classification (2026-07-15)

What happened -> The Mcu/Adc descriptor sidecars regenerated byte-for-byte from the exact cached descriptors (Mcu 489 items, Adc 279 items) and the offline consistency gate passed, but cold review found that the separately maintained classification overrides were name-regex inventories fitted to selected leaf fields rather than complete implementation traces. The Mcu rule marked seven `McuPll_1` items as derived even though the fixed recipe edits only `McuPll_0`, marked `McuPll_Parameter` containers derived even though the apply contract explicitly preserves them untouched, and left actual recipe writes such as `McuPLLEnabled` and `McuCgm0ClockMux0/McuClkMux0_Source` deferred. Adc similarly left caller-created/edited structural surfaces such as `AdcHwUnit`, `AdcChannel`, `AdcGroup`, `AdcThresholdControl`, `AdcHwConfiguration`, and BCTU parent lists/containers deferred while classifying selected descendants configurable. At the same time, `_coverage` was deleted from BaseNXP, Mcl, and Platform runtime assets without corresponding development sidecars, erasing their prior configurable/deferred accounting; the core design and an Mcu provider docstring still required the obsolete runtime `_coverage`/`not_yet_exposed` model. Root cause -> Source extraction truth and implementation-classification truth were treated as one green gate. The gate proved exact descriptor facts, unique keys, trace-symbol existence, and summary arithmetic, but did not prove that every actual provider/apply write maps to configurable/derived items, that every claimed item is reached by that implementation, or that moving coverage out of runtime preserved every module's existing evidence. Broad terminal-name regexes also discarded structural ancestry, so same-named PLL0/PLL1 fields collapsed onto one claim. Durable guard -> Make classification path-specific and bidirectional: derive an implementation write inventory (including created/removed list/container structure and vendor-derived fields), require every write to resolve to a non-deferred structural key, and require every configurable/derived key to have field-specific trace evidence that reaches that exact path. Overrides for repeated names must include ancestor-qualified paths (for example `McuPll_0`, never a bare terminal-name regex), with negative tests for sibling instances such as `McuPll_1`. Treat coverage relocation as a lossless migration: before deleting any runtime `_coverage`, commit and validate the corresponding module sidecar; repository validation must fail when a shipped provider/module lacks one. Update the authoritative core design and module documentation in the same change so runtime assets, development sidecars, and the written build sequence describe one model. Descriptor regeneration is necessary evidence of source fidelity, never evidence of honest implementation coverage by itself.

## LL-037 - A Verified Payload Still Escapes Through an Unverified Deployment Ancestor (2026-07-15)

What happened -> The release manifest, exact tracked-file boundary, source/staging hashes, same-version drift handling, rollback tests, immutable launchers, and cross-platform CI were green, but cold review found that the deployer verified links and Windows reparse points only inside the Skill payload. It created locks, staging directories, rollback directories, and the final installation through `<target>/.agents/skills` without first proving that each destination ancestor below the selected target was a real directory. A pre-existing POSIX symlink or Windows junction/reparse at `.agents` or `.agents/skills` could therefore redirect the entire verified transaction outside the requested target; hashing every copied byte does not constrain where those bytes are published. The README also described a release boundary that omitted the actually published `reference/` directory and promised a Windows junction fallback the implementation did not provide. Root cause -> Content integrity and location integrity were reviewed as if one implied the other. The manifest made the source and staged trees exact, while path construction still used ordinary `mkdir`, `rename`, and `resolve` semantics that follow ancestor links. Tests injected links into payload files but never into a writable destination ancestor, so they proved no-follow traversal within the payload rather than containment of the publication root. Durable guard -> Establish a trusted deployment-root capability before any lock or staging mutation, then walk every existing child component with no-follow metadata checks and reject symlinks/reparse points; create missing components one at a time and revalidate them before use. Pin source, staging, previous, destination, and lock paths to that verified parent and add a POSIX real-symlink test plus Windows real/injected reparse-ancestor tests asserting that the external target and existing installation remain unchanged. Treat content integrity and path containment as separate mandatory gates. Documentation must enumerate the exact manifest payload and describe only link/fallback behavior actually implemented. The uniform-file-header template's intentional trailing spaces remain authoritative and are not diff-hygiene defects.

## LL-038 - Acceptance Evidence Must Stay Agent-Agnostic (2026-07-18)

What happened -> The P1 #65–#72 implementation and every functional/KPI gate were green, but cold review found that the refreshed Category A acceptance report recorded orchestration details such as KPI-optimization iteration numbers, provider queueing, and a deployed-agent self-patch; the core-design metadata and README evidence date also lagged their newly committed 0.7.2/0.35.0 evidence. Root cause -> Durable engineering evidence was updated by copying the Agent execution narrative instead of reducing it to product-observable inputs, outputs, measurements, and validation results, while document metadata/backlinks were not treated as part of the same atomic documentation change. Durable guard -> Category A acceptance records state only case identity, fixture, public-tool result, KPI measurement, edit count, independent vendor evidence, and engineering disposition; iteration routing, queues, agent self-repair, and role/process details belong only in Category B. Whenever a Category A document version/date advances, review its metadata, newest changelog row, and every README/backlink label together, and run the repo-wide Category A purity lexicon before approval.

## LL-039 - Removing a Superseded Rule Must Not Rewrite Its History (2026-07-18)

What happened -> The normalized v2 coverage migration was lossless against all eight cached XDM descriptors and preserved every prior classification, gap, and trace while substantially reducing committed size, but the same green change replaced the governance changelog's existing 0.1.5 row with 0.1.6 when deleting the superseded committed-plan directory. Root cause -> Removing an obsolete current-state map entry was incorrectly extended to removing the historical row that recorded when that entry was introduced. Durable guard -> Treat current-state maps and append-only history as separate projections: update or remove obsolete map entries, append a new reversal/supersession row, and require a test that every pre-change changelog row remains an unchanged ordered subsequence after any documentation version bump.
