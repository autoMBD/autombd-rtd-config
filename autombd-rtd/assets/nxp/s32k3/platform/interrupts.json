{
  "_identity": {"vendor":"NXP","family":"S32K3","device":"S32K344","package":"mapbga257","rtd_release":"7.0.1","module":"Platform","role":"interrupt_schema"},
  "family": "S32K3",
  "device": "S32K344",
  "rtd_version": "7.0.1",
  "source": "RTD/Platform_TS_T40D34M70I1R0/config/Platform.xdm",
  "isr_priority": {
    "min": 0,
    "max_ref": "Platform.irqMaxPrio",
    "note": "Platform.xdm IsrPriority INVALID range: >=0 AND <= ecu:get('Platform.irqMaxPrio'). The device-specific upper bound is a processor feature value, authoritatively enforced by the S32DS validation gate; the provider enforces the known lower bound."
  },
  "isr_name_patterns": {
    "LPUART_<n>": "LPUART<n>_IRQn",
    "FLEXIO_<x>": "FLEXIO_IRQn",
    "note": "PlatformIsrConfig.IsrName domain is ecu:list('Platform.irqsList'). These patterns cover the minimal-system peripherals and are confirmed against Uart_Example_S32K344 (LPUART3_IRQn, FLEXIO_IRQn). The provider derives a candidate name then verifies it exists in the project's PlatformIsrConfig before editing -- it never invents an entry."
  }
}
